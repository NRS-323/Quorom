@using Microsoft.AspNetCore.Identity

@inject SignInManager<QuoromUser> SignInManager
@inject UserManager<QuoromUser> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item">
            <a class="nav-link" asp-controller="Account" asp-action="Login">@UserManager.GetUserName(User)</a>
        </li>
        /*
        <li class="nav-item">
            <form id="logoutForm" method="post" class="form-inlne" asp-controller="Account" asp-action="Logout">
                <button type="submit" class="btn btn-warning btn-rounded btn-icon text-light">
                    <i class="mdi mdi-logout"></i>
                </button>
            </form>
        </li>
        */
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-light" asp-controller="Account" asp-action="Register">Register</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-light" asp-controller="Account" asp-action="Login">Login</a>
        </li>
    }
</ul>
